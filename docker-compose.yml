version: '4.0'
services:
  db:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: subscribe_db
    volumes:
      - ./data:/var/lib/postgresql/data
    ports:
      - 5432:5432